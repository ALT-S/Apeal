{% extends '::layout-back.html.twig' %}

{% set element = form.vars.value %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <a class="navbar-brand" href="{{ path('admin_ms_dashboard') }}" style="padding-bottom:0 ;padding-left: 0">
                {% trans %}Dashboard MS{% endtrans %}
            </a>
            <span class=" glyphicon glyphicon-arrow-right" style="top: 18px;    float: left;"></span>
            <a class="{% if app.request.attributes.get('_route') == 'admin_ms_creation_musique' %}navbar-brand{% endif %}
    " href="{{ path('admin_ms_creation_musique'  )}} " style="color: red">
                {% trans %}Ajout d'une Musique{% endtrans %}</a>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-12">
            <h2>{% trans %}Ajout d'une Musique{% endtrans %}</h2>
        </div>
    </div>
    <hr>
    <div class="row" style="padding-top: 45px">
        <div class="col-lg-6">

            {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}

            <div class="form-group">
                {{ form_label(form.title, "Titre :" ) }}
                <div class="col-sm-10">
                    {{ form_widget(form.title) }}
                    {{ form_errors(form.title) }}
                </div>
            </div>
            <div class="form-group">
                {{ form_label(form.date, "Date :" ) }}
                <div class="col-sm-10">
                    {{ form_widget(form.date) }}
                    {{ form_errors(form.date) }}
                </div>
            </div>
            <div class="form-group">
                {{ form_label(form.lien, "Lien http :" ) }}
                <div class="col-sm-10">
                    {{ form_widget(form.lien) }}
                    {{ form_errors(form.lien) }}
                </div>
            </div>
            {{ form_rest(form) }}

            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}